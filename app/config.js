// Central API base URL. Change this when running backend in Docker / emulator / device.
// Android emulator (default): 10.0.2.2
// Genymotion: 10.0.3.2
// iOS Simulator: localhost
// Physical device: use your PC IP like http://192.168.1.100:3000
export const BASE_URL = 'http://localhost:3000';

// Default export stub so expo-router doesn't treat this file as a route.
const _config_default_stub = null;
export default _config_default_stub;
